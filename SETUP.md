# 🚀 Настройка Telegram бота

## Шаг 1: Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Payment Analytics Bot")
   - Введите username бота (должен заканчиваться на `bot`, например: `payment_analytics_bot`)
4. Скопируйте полученный токен (выглядит примерно так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Создание .env файла

Скопируйте пример конфигурации:

```bash
cd telegram_bot
cp .env.example .env
```

Откройте `.env` и заполните:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
BACKEND_URL=http://localhost:3000
RUST_LOG=info,telegram_bot=debug
```

### Объяснение переменных:

- **TELEGRAM_BOT_TOKEN** (обязательно) - токен бота от @BotFather
- **BACKEND_URL** (опционально) - URL бэкенда, по умолчанию `http://localhost:3000`
- **RUST_LOG** (опционально) - уровень логирования, по умолчанию `info`

## Шаг 3: Убедитесь, что бэкенд запущен

Перед запуском бота убедитесь, что Payment Analytics Backend работает:

```bash
# В корне проекта
cd ..
cargo run
# или если используете Docker
docker-compose up
```

Проверьте, что бэкенд доступен:

```bash
curl http://localhost:3000/api/health
```

Должен вернуться JSON с `"status": "ok"`.

## Шаг 4: Запуск бота

```bash
cd telegram_bot
cargo run
```

Если все настроено правильно, вы увидите:

```
Bot is starting...
Backend is available
```

## Шаг 5: Тестирование

1. Найдите вашего бота в Telegram (по username, который вы указали при создании)
2. Отправьте команду `/start`
3. Попробуйте запрос: "Сколько транзакций было сегодня?"

## 🔧 Устранение проблем

### Ошибка: "TELEGRAM_BOT_TOKEN environment variable is required"
- Убедитесь, что файл `.env` существует в директории `telegram_bot/`
- Проверьте, что токен указан правильно (без пробелов, кавычек)

### Ошибка: "Backend is not available"
- Проверьте, что бэкенд запущен
- Проверьте URL в `BACKEND_URL` (должен быть доступен)
- Если бэкенд на другом хосте, укажите полный URL (например: `http://192.168.1.100:3000`)

### Бот не отвечает
- Проверьте логи бота на наличие ошибок
- Убедитесь, что токен правильный
- Проверьте, что бэкенд работает и доступен

## 📝 Пример .env файла

```env
# Минимальная конфигурация
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
BACKEND_URL=http://localhost:3000

# С дополнительными настройками
RUST_LOG=debug,telegram_bot=trace
```

